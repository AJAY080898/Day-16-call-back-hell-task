var times = document.getElementById("time")
let display = 10;
setTimeout(() => {
    times.innerText = display--

    setTimeout(() => {
        times.innerText = display--

        setTimeout(() => {
            times.innerText = display--

            setTimeout(() => {
                times.innerText = display

                setTimeout(() => {
                    times.innerText = display--

                    setTimeout(() => {
                        times.innerText = display--

                        setTimeout(() => {
                            times.innerText = display--

                            setTimeout(() => {
                                times.innerText = display--


                                setTimeout(() => {
                                    times.innerText = display--

                                    setTimeout(() => {
                                        times.innerText = display--

                                        setTimeout(() => {
                                            times.innerText = display--

                                            setTimeout(() => {
                                                times.innerText = "Happy Independance Day"

                                            }, 1000)
                                        }, 1000)
                                    }, 1000)

                                }, 1000)
                            }, 1000)
                        }, 1000)
                    }, 1000)
                }, 1000)
            }, 1000)
        }, 1000)
    }, 1000)
}, 1000)
